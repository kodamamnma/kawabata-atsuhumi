name: Deploy to Production

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    - name: PHP Syntax Check
      uses: ./.github/actions/php-syntax-check
  
  deploy:
    needs: test
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up SSH
      uses: webfactory/ssh-agent@v0.9.0
      with:
        ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY_PRODUCTION }}

    - name: Add server to known_hosts
      run: |
        SSH_HOST="${{ secrets.SERVER_HOST }}"
        ssh-keyscan -p 10022 -H "${SSH_HOST}" >> ~/.ssh/known_hosts

    - name: Ensure target theme directory exists on remote
      run: |
        SSH_HOST="${{ secrets.SERVER_HOST }}"
        SSH_USER="${{ secrets.SERVER_USER }}"
        THEME_PATH="${{ secrets.PRODUCTION_THEME_PATH }}"
        ssh -p 10022 -o StrictHostKeyChecking=no "${SSH_USER}"@"${SSH_HOST}" "mkdir -p \"${THEME_PATH}\""

    - name: Rsync theme files to PRODUCTION
      run: |
        SSH_HOST="${{ secrets.SERVER_HOST }}"
        SSH_USER="${{ secrets.SERVER_USER }}"
        THEME_PATH="${{ secrets.PRODUCTION_THEME_PATH }}"
        rsync -avzc --delete --exclude='.*' --exclude README.md -e "ssh -p 10022 -o StrictHostKeyChecking=no" ./ "${SSH_USER}"@"${SSH_HOST}":"${THEME_PATH}"

    - name: Clear WordPress Cache
      run: |
        SSH_HOST="${{ secrets.SERVER_HOST }}"
        SSH_USER="${{ secrets.SERVER_USER }}"
        ssh -p 10022 -o StrictHostKeyChecking=no "${SSH_USER}"@"${SSH_HOST}" "wp w3-total-cache flush all"

  release:
    needs: deploy
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    - name: Release Drafter
      uses: release-drafter/release-drafter@v6
      with:
        publish: true
        version: ${{ steps.version.outputs.version-without-v }}
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}